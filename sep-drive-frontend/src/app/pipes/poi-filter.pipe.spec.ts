import { PoiFilterPipe } from './poi-filter.pipe';

describe('PoiFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PoiFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
